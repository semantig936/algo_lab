#include<stdio.h>
#include<stdlib.h>
#include<string.h>

int main()
{
    int n;
    printf("Enter size of string: ");
    scanf("%d",&n);
    char* str = (char*)malloc(n*sizeof(char));
    printf("Enter the string: ");
}